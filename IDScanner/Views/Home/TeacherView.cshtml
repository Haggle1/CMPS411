@model IDScanner.ViewModels.TeacherViewModel

@{
    ViewBag.Title = "TeacherView";
}
<center>
    <h2>
        @Html.DisplayName("Current Time you know this works:")
        <br />
        @Html.DisplayName(DateTime.Now.ToShortTimeString())
        <br />
        @Html.DisplayName(DateTime.Now.ToLocalTime().ToString())
        <br />
        @Html.DisplayName(DateTime.Now.ToString())
        <br />
         @Html.DisplayName(DateTime.Today.ToLongTimeString())
        <br />
    </h2> 
    
    

            @foreach(var cour in Model.Course){
               foreach (var teach in Model.Faculty){
                    @:<h2>
                         @Html.DisplayName("Current Time you know this works:")
                    if(teach.FID == cour.FID && DateTime.Now > cour.TimeStart && DateTime.Now < cour.TimeEnd){
                        <br />
                 @Html.DisplayName("should:")
                        @Html.DisplayFor(modelItem => teach.Fac_Fname)
                        @Html.DisplayFor(modelItem => teach.Fac_Lname)
                        @:'s  
                        @Html.DisplayFor(modelItem => cour.CourseName)
                        @: class.
                        <br />
                        @Html.DisplayName(cour.TimeStart.ToShortTimeString())       
                        @: to
                        @Html.DisplayName(cour.TimeEnd.ToShortTimeString())
                        @:</h2>
                        <br />


                        <div class="row">
                        <div class="col-md-4">
                        <table class="table">
                        <tr>
                        <th>
                          @Html.Label("Student Name")
                        </th>
                        <th>
                          @Html.Label("Time In")
                        </th>
                        <th>
                          @Html.Label("Time Out")
                        </th>
                        <th></th>
                        </tr>
                                

                        @foreach (var item in Model.Student)
                            {
                               foreach (var sched in Model.Schedule)
                                {
                                if(sched.ID == item.SID && cour.CID == sched.CourseID )
                                    {
                                    <tr>
                                    <td>
                                    @Html.DisplayFor(modelItem => item.Fname)


                                    @Html.DisplayFor(modelItem => item.Lname)
                                    </td>
                                    <td>
                                    @Html.DisplayFor(modelItem => item.Attendance.Clock_In)
                                    </td>
                                    <td>
                                    @Html.DisplayFor(modelItem => item.Attendance.Clock_Out)
                                    </td>

                                    </tr>
                                    }
                                }


                            }
                    </table>
                    </div>
                    <div class="col-md-4">
                    </div>

                    <div class="col-md-4">
            
           
            @*<table class="table">
                <tr>
                    <th>
                        @Html.Label("Absent")
                    </th>
            
                    <th></th>
                </tr>

                @foreach (var item in Model.Student)
                {
                    foreach (var sched in Model.Schedule)
                    {
                        if (sched.ID == item.SID && cour.CID == sched.CourseID)
                        {
                            foreach (var atten in Model.Attendance)
                            {
                                if (atten.Clock_In != null)
                                {

                                    <tr>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Fname)

                                            @Html.DisplayFor(modelItem => item.Lname)
                                        </td>
                                        <td></td>
                                        <td></td>

                                    </tr>
                                }


                            }

                        }
                    }


                }
            </table>*@ 


        </div>
    </div>

                   }<!--end fid==cid -->

               }<!--End foreach teacher in model-->
            <br />
                <!--End foreach cour model-->
            }

        
            
            
            
      
   
</center>

