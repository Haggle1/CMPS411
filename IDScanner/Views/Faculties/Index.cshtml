@model IEnumerable<IDScanner.Faculty>

@{
    ViewBag.Title = "Index";
}

<h2>Faculty</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("First Name")
        </th>
        <th>
            @Html.DisplayName("Last Name")
        </th>
        <th>

        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fac_Fname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fac_Lname)
        </td>

        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FID })
        </td>
    </tr>
}

</table>
